//factorial of any number using recurtion.



#include<stdio.h>
#include<conio.h>
long long int fact(long long int n)
{
if(n>=1)
return n*fact(n-1);
else
return 1;
}
int main()
{
long long int x, y;
printf("enter any number\n");
scanf("%ld",&x);
y=fact(x);
printf("%ld",y);
return(0);
}
